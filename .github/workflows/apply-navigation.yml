name: Apply navigation and sitemap

on:
  push:
    branches:
      - site-navigation-and-sitemap
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  update-navigation:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out navigation branch
        uses: actions/checkout@v4
        with:
          ref: site-navigation-and-sitemap
          fetch-depth: 0

      - name: Generate and normalize navigation
        run: |
          python tools/update_navigation.py
          python tools/fix_navigation.py

      - name: Commit generated navigation
        run: |
          if git diff --quiet -- . ':!.github/workflows/apply-navigation.yml' ':!tools/update_navigation.py' ':!tools/fix_navigation.py'; then
            echo "No generated changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add README.md SITEMAP.md course reference glossary research docs
          git commit -m "Normalize site-wide navigation placement"
          git push origin HEAD:site-navigation-and-sitemap
